.title
  margin: 0
  // margin 16px 0;
  font-weight bold;
  font-size 28px;
  line-height 130%;
  height 14vh

  @media $mobile-size
    // margin 36px 0;
    font-weight normal;
    font-size 50px;

.subtitle
  font-size 26px;

  @media $mobile-size
    font-size 48px;
    line-height 130%;

.default-text
  font-weight bold;

  @media $mobile-size
    font-weight normal;
