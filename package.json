{
  "scripts": {
    "dev": "export NODE_ENV=dev && gulp watch",
    "build": "export NODE_ENV=prod && gulp watch"
  },
  "browserslist": [
    "last 4 version"
  ],
  "devDependencies": {
    "autoprefixer": "^9.7.4",
    "cssnano": "^4.1.10",
    "gulp": "^4.0.2",
    "gulp-postcss": "^8.0.0",
    "gulp-pug": "^4.0.1",
    "gulp-stylus": "^2.7.0"
  }
}
